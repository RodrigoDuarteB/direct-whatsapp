import { ImageStyle } from 'react-native';
import { TextStyle } from 'react-native';
import { StyleProp } from 'react-native';
import { ViewStyle } from 'react-native';

export default {
    flexRow: {
        flexDirection: 'row'
    }
} as StyleProp<ViewStyle | TextStyle | ImageStyle>